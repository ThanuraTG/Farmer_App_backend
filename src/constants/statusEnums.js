const PLAN_STATUSES = ['planned', 'active', 'harvesting', 'completed', 'cancelled'];

const ACCOUNT_STATUSES = ['active', 'inactive', 'suspended'];

const NOTIFICATION_TYPES = ['weather', 'market', 'supply', 'cultivation', 'general'];

const LAND_UNITS = ['acres', 'hectares', 'perches'];

const CROP_STATUSES = ['active', 'inactive'];

module.exports = {
  PLAN_STATUSES,
  ACCOUNT_STATUSES,
  NOTIFICATION_TYPES,
  LAND_UNITS,
  CROP_STATUSES
};
